<% content_for(:title, "#{@service.persisted? ? "Edit" : "New"} Service - Admin") %>

<div class="max-w-2xl">
  <h1 class="font-serif text-3xl font-semibold text-theme-500 mb-8">
    <%= @service.persisted? ? "Edit service" : "Add service" %>
  </h1>
  <%= render "form", service: @service %>
</div>