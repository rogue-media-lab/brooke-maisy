<% content_for(:title, "Add Line Item — Quote ##{@quote.id}") %>
<div class="max-w-2xl mx-auto">
  <div class="mb-6"><%= link_to admin_quote_path(@quote), class: "text-sm text-gray-400 hover:text-theme-500" do %>← Quote<% end %></div>
  <h1 class="font-serif text-2xl font-semibold text-theme-500 mb-8">Add Line Item</h1>
  <div class="bg-white rounded-xl border border-theme-100 p-8"><%= render "form" %></div>
</div>