<% content_for(:title, "Edit Checklist Item - Admin") %>

<div class="mb-8">
  <%= link_to admin_checklist_items_path, class: "text-sm text-gray-500 hover:text-theme-500" do %>
    ← Back to checklist
  <% end %>
  <h1 class="font-serif text-3xl font-semibold text-theme-500 mt-2">Edit <%= @checklist_item.name %></h1>
</div>

<%= render "form", checklist_item: @checklist_item %>