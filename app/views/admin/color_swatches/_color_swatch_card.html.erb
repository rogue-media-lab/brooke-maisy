<div class="flex items-center gap-3 bg-white rounded-lg border border-theme-100 p-3" id="<%= dom_id(swatch) %>">
  <div class="w-12 h-12 rounded-md border border-gray-200 flex-shrink-0" style="background-color: <%= swatch.hex_code %>;"></div>
  <div class="flex-1 min-w-0">
    <h3 class="font-medium text-sm text-theme-500 truncate"><%= swatch.name %></h3>
    <p class="text-xs text-gray-400">
      <%= swatch.hex_code %>
      <%= "· #{swatch.brand}" if swatch.brand.present? %>
      <%= "· #{swatch.finish}" if swatch.finish.present? %>
    </p>
  </div>
  <div class="flex items-center gap-1 flex-shrink-0">
    <%= button_to move_admin_project_design_presentation_color_swatch_path(project, presentation, swatch), method: :patch,
          params: { direction: "up" },
          class: "text-gray-300 hover:text-theme-500 bg-transparent border-0 p-1 cursor-pointer" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" /></svg>
    <% end %>
    <%= button_to move_admin_project_design_presentation_color_swatch_path(project, presentation, swatch), method: :patch,
          params: { direction: "down" },
          class: "text-gray-300 hover:text-theme-500 bg-transparent border-0 p-1 cursor-pointer" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
    <% end %>
    <%= link_to edit_admin_project_design_presentation_color_swatch_path(project, presentation, swatch),
          class: "text-gray-300 hover:text-theme-500 p-1", data: { turbo_frame: "color_swatch_form" } do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" /></svg>
    <% end %>
    <%= button_to admin_project_design_presentation_color_swatch_path(project, presentation, swatch), method: :delete,
          form: { data: { turbo_confirm: "Delete this swatch?" } },
          class: "text-gray-300 hover:text-red-500 bg-transparent border-0 p-1 cursor-pointer" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
    <% end %>
  </div>
</div>
