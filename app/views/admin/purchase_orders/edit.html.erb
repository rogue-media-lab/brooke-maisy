<% content_for(:title, "Edit PO ##{@purchase_order.id}") %>

<div class="max-w-2xl mx-auto">
  <%= link_to admin_purchase_order_path(@purchase_order), class: "text-sm text-gray-400 hover:text-theme-500 mb-6 inline-block" do %>← PO #<%= @purchase_order.id %><% end %>
  <h1 class="font-serif text-2xl font-semibold text-theme-500 mb-8">Edit Purchase Order</h1>

  <div class="bg-white rounded-xl border border-theme-100 p-8">
    <%= render "form" %>
  </div>
</div>