<% content_for(:title, "New Presentation") %>

<%= link_to admin_project_design_presentations_path(@project), class: "inline-flex items-center gap-1.5 text-sm text-gray-500 hover:text-theme-500 mb-4" do %>
  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
  All presentations
<% end %>

<h1 class="font-serif text-3xl font-semibold text-theme-500 mb-6">New Presentation</h1>

<%= render "form", project: @project, presentation: @presentation %>
